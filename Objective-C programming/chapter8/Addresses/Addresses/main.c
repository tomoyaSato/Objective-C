//
//  main.c
//  Addresses
//
//  Created by Minami Kyohei on 2016/06/01.
//  Copyright © 2016年 Minami Kyohei. All rights reserved.
//

#include <stdio.h>

int main(int argc, const char * argv[]) {
    /*
    int i = 17;
    int *addressOfI = &i; //iのアドレスをポインタ変数addressOfIに代入
    printf("i stores its value at %p\n", addressOfI);
    printf("the in stored at addressOfI is %d\n", *addressOfI);//逆参照演算子*でこのアドレスに格納している値を取得
    *addressOfI = 89;//この場合addresOfIが示すアドレスへ89を代入
    printf("Now i is %d\n", i); //iが格納されているアドレス（addressOfi）が89になったのでi = 89となる
    printf("An int is %zu bytes \n", sizeof(int));
    printf("A pointer is %zu bytes \n", sizeof(int *));
    */
    
    /*
    float *myPointer;
    myPointer = NULL;//今後なんらかのアドレスを格納したい場合にはnullを格納しておく
    
    float *x;
    x　= NULL;
    float* y = NULL; //これらは同じようにポインタだが
    
    float* a, b; //2つ以上となるとaはポインタだが、bはfloat変数となってしまう
                 //変数の前に*をつける癖をつける
    */
    
    return 0;
}
